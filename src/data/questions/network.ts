import type { RawQuestion } from '../../types'

/**
 * 计算机网络题库
 * 来源：docs/计算机网络面试题.md、docs/一些高频率考点.md、
 *       docs/收集的面试知识点.md、docs/知识点快速复习指南.md
 */
export const networkQuestions: RawQuestion[] = [
  // ============ HTTP ============
  {
    id: 'net-001',
    type: 'essay',
    diff: 'easy',
    sub: 'HTTP 协议',
    q: 'GET 和 POST 请求有哪些区别？',
    ans: `**面试回答：**

1. **语义**：GET 用于**获取资源**，幂等、安全（不改变服务器状态）；POST 用于**提交数据/创建资源**，不幂等；
2. **参数位置**：GET 参数放在 **URL** 中，长度受 URL 限制（浏览器限制约 2KB~8KB）；POST 参数放在**请求体**中，理论上不受限；
3. **缓存与收藏**：GET 可以被浏览器缓存、收藏为书签、保留在历史记录；POST 不能；
4. **编码类型**：GET 只支持 URL 编码；POST 支持多种（form-data、application/json 等）；
5. **安全性**：都不加密，但 GET 参数直接暴露在 URL，敏感信息不能用 GET。

补充：GET 也会携带请求体（RFC 未禁止）但服务端通常忽略；GET 产生一个 TCP 数据包（头+数据一起），POST 可能两个包（先头发包，部分浏览器实现），这个说法有争议，面试提及即可。`,
    ana: '幂等性是加分点：GET 幂等，POST 不幂等，PUT 幂等。',
    keys: ['幂等', 'URL 参数', '请求体', '缓存'],
    src: '计算机网络面试题.md',
  },
  {
    id: 'net-002',
    type: 'single',
    diff: 'medium',
    sub: 'HTTP 协议',
    q: 'POST 和 PUT 请求的核心区别是什么？',
    opts: [
      'POST 幂等，PUT 不幂等',
      'PUT 幂等（重复提交同一资源状态不变），POST 不幂等（每次都会新建资源）',
      'PUT 只能上传文件',
      'POST 请求体更小',
    ],
    ans: 'B',
    ana: 'PUT 语义是“**整体替换**指定资源”，客户端指定资源 URI，重复执行结果一致，**幂等**；POST 语义是“向集合**新建**资源”，服务端决定 URI，重复提交会创建多个资源，**不幂等**。PATCH 则是部分更新。',
    keys: ['PUT 幂等', '整体替换', 'POST 新建'],
    src: '计算机网络面试题.md / fastapi知识点.md',
  },
  {
    id: 'net-003',
    type: 'essay',
    diff: 'medium',
    sub: 'HTTP 协议',
    q: 'HTTP 1.0、1.1、2.0、3.0 各有哪些主要区别？',
    ans: `**面试回答：**

**HTTP/1.0 → 1.1**：
1. **长连接**：1.0 默认短连接（每次请求都建 TCP），1.1 默认 \`keep-alive\` 复用连接；
2. **管线化**：可以串行发出多个请求（但响应仍按序，队头阻塞仍在）；
3. 新增 Host 头（支持虚拟主机）、缓存控制（Cache-Control）、分块传输（chunked）等。

**HTTP/1.1 → 2.0**：
1. **二进制分帧**：数据以二进制帧传输；
2. **多路复用**：一个 TCP 连接上并行多个**流**，解决 HTTP 层队头阻塞（TCP 层的还在）；
3. **头部压缩 HPACK**：静态表+动态表+哈夫曼编码，减少重复头开销；
4. **服务器推送**（Server Push）。

**HTTP/2.0 → 3.0**：
1. 传输层从 TCP 换成 **QUIC（基于 UDP）**；
2. **彻底解决 TCP 队头阻塞**（流级别独立重传）；
3. **连接建立更快**：QUIC 把传输和加密握手合并（1-RTT 甚至 0-RTT）；
4. **连接迁移**：用 Connection ID 标识连接，切换网络（WiFi→4G）不断线。

一句话演进：**短连接 → 长连接 → 多路复用 → QUIC/UDP**。`,
    ana: '队头阻塞是贯穿线索：HTTP 层的队头阻塞被 2.0 多路复用解决；TCP 层的队头阻塞由 3.0 QUIC 解决。',
    keys: ['keep-alive', '多路复用', 'HPACK', 'QUIC'],
    src: '计算机网络面试题.md',
  },
  {
    id: 'net-004',
    type: 'essay',
    diff: 'easy',
    sub: 'HTTP 协议',
    q: 'HTTP 和 HTTPS 的区别是什么？HTTPS 是如何保证安全的？',
    ans: `**面试回答：**

**区别**：
1. HTTPS = **HTTP + TLS/SSL**，默认端口 443（HTTP 是 80），数据**加密传输**；
2. HTTPS 需要向 CA 申请**数字证书**，验证服务器身份；
3. HTTPS 握手有额外开销，但现代硬件下性能可接受。

**TLS/SSL 工作原理**：**混合加密 + 数字证书 + 摘要算法**：
- **对称加密**（会话密钥）加密通信内容，性能好；
- **非对称加密**（公私钥）仅用于**交换对称密钥**，解决密钥配送问题；
- **数字证书**：CA 用自己的私钥签发服务器公钥信息，防止中间人替换公钥；
- **摘要算法**（如 SHA-256）验证报文完整性，防篡改。

**握手过程简述**：客户端发随机数+支持的加密套件 → 服务端返回证书+随机数 → 客户端验证证书，生成预主密钥用公钥加密发送 → 双方用三个随机数生成会话密钥 → 之后用对称加密通信。

**面试简答**：HTTPS = HTTP + TLS，不是换了一套应用层协议，而是在传输前多了加密和身份校验。`,
    ana: '对称加密快但密钥难配送、非对称加密安全但慢 → 混合加密的动机要讲清。',
    keys: ['TLS 握手', '对称+非对称混合', 'CA 证书', '摘要防篡改'],
    src: '计算机网络面试题.md / 知识点快速复习指南.md',
  },
  {
    id: 'net-005',
    type: 'essay',
    diff: 'easy',
    sub: 'HTTP 状态码',
    q: '常见的 HTTP 状态码有哪些？302、303、307 有什么区别？',
    ans: `**面试回答：**按类别记：

- **2xx 成功**：200 OK；201 Created（资源创建成功，常用于 POST）；204 No Content（成功但无返回体，常用于 DELETE）；206 Partial Content（断点续传/分片下载）；
- **3xx 重定向**：301 永久重定向（搜索引擎更新索引）；302 临时重定向；304 Not Modified（协商缓存命中）；
- **4xx 客户端错误**：400 参数/格式错误；401 未认证（需登录）；403 已认证但无权限；404 资源不存在；405 方法不允许；413 请求体过大；429 触发限流；
- **5xx 服务端错误**：500 服务器内部错误；502 网关收到无效响应（如 Nginx 连不上后端）；503 服务不可用/过载；504 网关超时。

**302 / 303 / 307 区别**：
- **302**：临时重定向，规范允许改变方法但浏览器大多按 303 处理（转 GET），历史遗留混乱；
- **303**：重定向且**必须用 GET** 访问新地址（常用于 POST 后跳结果页）；
- **307**：临时重定向且**不允许改变请求方法**（POST 必须仍用 POST）；
- 对应地，308 是“不允许改方法的 301”。

**304 多好还是少好**：适度多好——304 说明协商缓存命中，减少了传输；但如果该缓存的内容频繁变化，过多的 304 校验请求也是浪费。`,
    ana: '重点记 301 vs 302 vs 307 的“是否改方法”，以及 401 vs 403 的“未认证 vs 无权限”。',
    keys: ['2xx/3xx/4xx/5xx', '304 协商缓存', '307 不改方法', '401 vs 403'],
    src: '计算机网络面试题.md / 一些高频率考点.md',
  },
  {
    id: 'net-006',
    type: 'single',
    diff: 'medium',
    sub: 'HTTP 协议',
    q: 'HTTP/2.0 的头部压缩使用的是什么算法？',
    opts: ['gzip 对整个报文压缩', 'HPACK：静态表 + 动态表 + 哈夫曼编码', 'Brotli 压缩', 'base64 编码缩短'],
    ans: 'B',
    ana: 'HTTP/2 采用 **HPACK**：①静态表记录常用头（如 :method GET）；②动态表记录连接期间出现过的自定义头；③哈夫曼编码压缩字符串。三者配合避免每次重复传输相同的头部字段，大幅减少头开销。HTTP/3 的 QUIC 则使用 QPACK，思路类似但适配了流乱序到达的场景。',
    keys: ['HPACK', '动态表', '哈夫曼编码'],
    src: '计算机网络面试题.md',
  },
  {
    id: 'net-007',
    type: 'essay',
    diff: 'medium',
    sub: 'HTTP 协议',
    q: '当页面有多张图片时，HTTP 1.1 和 HTTP 2.0 的加载表现有什么不同？对 keep-alive 的理解是什么？',
    ans: `**面试回答：**

**HTTP/1.1**：同一域名下**并发连接数有限**（Chrome 一般 6 个），多张图片需要排队复用连接，超出并发的请求会阻塞等待；所以有多域名分片（domain sharding）、雪碧图等优化手段。

**HTTP/2.0**：**一个 TCP 连接多路复用**，所有图片请求可以在同一连接上并行交错传输，互不阻塞，不再需要域名分片和雪碧图。

**keep-alive 的理解**：HTTP/1.1 默认开启的长连接机制。HTTP/1.0 每次请求都要“三次握手 → 请求 → 响应 → 四次挥手”，开销大；keep-alive 让一次 TCP 连接建立后可以**复用发送多次请求/响应**，减少握手与慢启动开销。注意 keep-alive 解决的是“**重复建连**”开销，但没有解决 HTTP 层的队头阻塞（同一连接上响应仍按序）。

结合项目：数据上云平台 SSE 长连接受 HTTP/1.1 同域 6 连接限制，多标签页会把请求阻塞，这是我用 BroadcastChannel 复用连接的问题背景。`,
    ana: '把 keep-alive 与多路复用区分开：一个省握手，一个解决并行。',
    keys: ['6 个并发连接', '多路复用', 'keep-alive 复用 TCP'],
    src: '计算机网络面试题.md / 针对简历问答.md',
  },
  {
    id: 'net-008',
    type: 'essay',
    diff: 'easy',
    sub: 'HTTP 协议',
    q: 'URL 有哪些组成部分？GET 的 URL 长度为什么会被限制？',
    ans: `**面试回答：**URL 组成：

\`\`\`
协议://域名:端口/路径?查询参数#锚点
https://example.com:443/api/v1/users?id=1#top
\`\`\`

1. **协议**：http/https/ftp 等；
2. **域名**：也可以是 IP，DNS 解析成 IP；
3. **端口**：http 默认 80，https 默认 443，省略时用默认端口；
4. **路径**：资源在服务器上的位置；
5. **查询参数**：? 后的 key=value，用 & 分隔；
6. **锚点（fragment）**：# 后的部分，**只在前端生效不会发给服务器**（前端路由 hash 模式的基础）。

**GET URL 长度限制的原因**：HTTP 协议本身**没有限制** URL 长度，限制来自**浏览器**（Chrome 约 2MB，但多数约定 2KB~8KB）和**服务器实现**（如 Nginx 默认 4KB~8KB 头缓冲）。POST 数据在请求体中，理论大小受服务器配置限制（如 413 Payload Too Large）。`,
    ana: '端口的作用：标识主机上的具体服务进程，一台服务器可以同时跑多个服务。',
    keys: ['协议/域名/端口/路径/参数/锚点', 'hash 不发给服务器'],
    src: '计算机网络面试题.md',
  },

  // ============ DNS / 网络模型 ============
  {
    id: 'net-009',
    type: 'essay',
    diff: 'medium',
    sub: 'DNS',
    q: 'DNS 是什么？完整的查询过程是怎样的？为什么同时使用 TCP 和 UDP？',
    ans: `**面试回答：**

**DNS**：域名系统，把 \`www.example.com\` 这类域名解析成 IP 地址，应用层协议。

**完整查询过程**（递归 + 迭代）：
1. 浏览器先查**自身 DNS 缓存**；
2. 查**操作系统缓存**和 hosts 文件；
3. 都没有则把请求发给**本地 DNS 服务器**（运营商，递归查询）；
4. 本地 DNS 没有就**迭代查询**：问根域名服务器（返回 .com 顶级域服务器地址）→ 问顶级域服务器（返回 authoritative 权威服务器地址）→ 问权威服务器拿到最终 IP；
5. 本地 DNS 缓存结果并返回给浏览器。

**为什么 TCP 和 UDP 都用**：
- **UDP（53 端口）**：常规查询用 UDP，一次往返就够，开销小速度快；
- **TCP**：响应报文超过 512 字节（区域传送、DNSSEC 大响应）时，或者查询被截断（TC 标志）后改用 TCP，保证传输可靠完整。

优化：\`dns-prefetch\` 可提前解析第三方域名，减少真正请求时的等待。`,
    ana: '递归查询（替你查到底）发生在客户端→本地 DNS；迭代查询（告诉你下一问谁）发生在本地 DNS→各级服务器。',
    keys: ['递归与迭代', '根/顶级/权威服务器', 'UDP 53', 'dns-prefetch'],
    src: '计算机网络面试题.md / 知识点快速复习指南.md',
  },
  {
    id: 'net-010',
    type: 'essay',
    diff: 'medium',
    sub: 'TCP 与 UDP',
    q: 'TCP 和 UDP 的区别是什么？各自的使用场景？',
    ans: `**面试回答：**

| 维度 | TCP | UDP |
|------|-----|-----|
| 连接 | 面向连接（三次握手） | 无连接 |
| 可靠性 | 可靠（确认重传、排序、流量/拥塞控制） | 尽力而为，不保证 |
| 传输方式 | 字节流 | 数据报文 |
| 传输效率 | 较低（开销大） | 高（头部 8 字节） |
| 一对多 | 仅一对一 | 支持广播、多播 |

**TCP 场景**：要求准确完整的场景——网页（HTTP/HTTPS）、文件传输（FTP）、邮件（SMTP）、SSH。
**UDP 场景**：要求实时、可容忍少量丢包——视频/语音通话、直播、DNS 查询、在线游戏，以及现代的 **QUIC/HTTP3**（在 UDP 上自行实现可靠传输）。

一句话：**TCP 换可靠，UDP 换实时**。`,
    ana: 'UDP 头只有 8 字节（TCP 至少 20 字节），这是效率高的原因之一。',
    keys: ['面向连接', '字节流 vs 数据报', '实时场景'],
    src: '计算机网络面试题.md',
  },
  {
    id: 'net-011',
    type: 'essay',
    diff: 'hard',
    sub: 'TCP 与 UDP',
    q: 'TCP 三次握手和四次挥手的过程？为什么是三次握手、四次挥手？',
    ans: `**面试回答：**

**三次握手（建立连接）**：
1. 客户端发 **SYN**（seq=x）：请求建立连接；
2. 服务端回 **SYN+ACK**（seq=y, ack=x+1）：同意并确认；
3. 客户端再发 **ACK**（ack=y+1）：连接建立。

**为什么必须三次**：双方都要确认“自己能发、对方能收、对方也能发、自己能收”。**两次不够**——若第二次就建立，服务端无法确认客户端有接收能力，且历史重复的 SYN 会让服务端白开无效连接（资源浪费）。

**四次挥手（断开连接）**：
1. 客户端发 **FIN**：我没有数据要发了；
2. 服务端回 **ACK**：知道了（此时服务端可能还有数据要发，所以先不关）；
3. 服务端发 **FIN**：我也发完了；
4. 客户端回 **ACK**，等待 **2MSL** 后进入 CLOSED。

**为什么四次**：因为 TCP 是全双工，两个方向的数据要**分别关闭**；服务端收到 FIN 后可能还有数据没发完，ACK 和 FIN 分开发，就多了一次。

**为什么等 2MSL**：确保最后一个 ACK 能到达（丢了服务端会重发 FIN），并让本连接的旧报文在网络中自然消亡，避免影响新连接。`,
    ana: '画时序图答题最稳。核心记忆：握手三次=确认双向收发能力；挥手四次=全双工分别关。',
    keys: ['SYN/SYN+ACK/ACK', '全双工', '2MSL', '半关闭'],
    src: '计算机网络面试题.md / 知识点快速复习指南.md',
  },
  {
    id: 'net-012',
    type: 'essay',
    diff: 'hard',
    sub: 'TCP 与 UDP',
    q: 'TCP 如何保证可靠传输？重传、流量控制、拥塞控制分别是怎么回事？',
    ans: `**面试回答：**TCP 通过校验、序号、确认、重传、流控、拥塞控制六种机制保证可靠：

**1. 重传机制**：
- **超时重传**：发出数据后启动定时器，RTO 时间内没收到 ACK 就重传；
- **快速重传**：收到 **3 个重复 ACK** 就不等超时，立即重传丢失报文；
- **SACK**（选择性确认）：告诉发送方哪些段收到了，只重传真正丢失的部分。

**2. 流量控制**：**接收方**通过窗口字段（rwnd）告知自己的**接收缓冲区**剩余能力，发送方发送速度不能超过它，防止接收方被撑爆。

**3. 拥塞控制**：针对**网络整体**拥塞，发送方维护拥塞窗口（cwnd）：
- **慢启动**：指数增长（每 RTT 翻倍）直到阈值；
- **拥塞避免**：超过阈值后线性加性增大；
- **拥塞发生**：超时则阈值减半、cwnd 重置为 1 重新慢启动；快速重传则减半进入快速恢复。

**4. 其他**：校验和（检错）、序列号（排序去重）、确认应答（ARQ）。

一句话：**流量控制怕压垮接收方，拥塞控制怕压垮网络**。`,
    ana: '三次重复 ACK 触发快速重传、cwnd 慢启动指数→拥塞避免线性，两个数字细节是加分项。',
    keys: ['超时/快速重传', '滑动窗口', '慢启动', '拥塞避免'],
    src: '计算机网络面试题.md',
  },
  {
    id: 'net-013',
    type: 'essay',
    diff: 'medium',
    sub: 'TCP 与 UDP',
    q: 'TCP 粘包是怎么回事？如何处理？为什么 UDP 不会粘包？',
    ans: `**面试回答：**

**粘包本质**：TCP 是**字节流协议，没有消息边界**。发送方多次写入的数据可能被合并成一个报文段发送（Nagle 算法），接收方也可能一次读到多个逻辑消息或半条消息。“粘包”不是 TCP 的 bug，而是**应用层没有定义消息边界**。

**处理方式（在应用层定义边界）**：
1. **固定长度**：每条消息定长，不足补齐；
2. **分隔符**：如 \\r\\n 分隔（HTTP 头、Redis 协议的做法）；
3. **长度前缀**：消息头带上消息体长度，先读长度再读内容（最通用，RPC 框架常用）。

**为什么 UDP 不粘包**：UDP 是**数据报协议**，每个 UDP 数据报有**天然边界**，发送几次就接收几个独立报文，应用层一次 recv 拿到一条完整消息（可能丢失或乱序，但不会粘连）。

前端关联：WebSocket 消息有帧边界，SSE 用空行分隔 event，也是“定义消息边界”的思路。`,
    ana: '关键词：字节流无边界、Nagle、长度前缀。UDP 面向报文保留边界。',
    keys: ['字节流无边界', '长度前缀', 'UDP 面向报文'],
    src: '计算机网络面试题.md',
  },

  // ============ WebSocket / SSE ============
  {
    id: 'net-014',
    type: 'essay',
    diff: 'medium',
    sub: 'WebSocket',
    q: '对 WebSocket 的理解？它和 HTTP 是什么关系？',
    ans: `**面试回答：**WebSocket 是一种在**单个 TCP 连接上全双工通信**的协议。

**与 HTTP 的关系**：
- 握手阶段借用 HTTP：客户端发带 \`Upgrade: websocket\`、\`Connection: Upgrade\` 头的 GET 请求，服务端返回 **101 Switching Protocols** 表示协议升级成功；
- 升级后不再走 HTTP 报文格式，双方都可以**主动推送**消息；
- 默认端口同为 80/443（ws/wss，wss 是 TLS 加密）。

**特点**：
1. **全双工**：服务端可主动推送，客户端可随时发送；
2. **低延迟低开销**：没有 HTTP 每次请求的头开销，帧很小；
3. **长连接**：需要心跳保活（ping/pong）与断线重连机制。

**适用场景**：聊天室、协同编辑、实时行情、游戏、工作流节点运行状态推送。我在农担项目的工作流调试链路用 WebSocket 承接 node_run、stream_msg 等事件，前端按 category/type 分发更新 UI 状态。`,
    ana: '101 状态码是握手的关键；“HTTP 只能请求-响应，WS 双向主动”是本质区别。',
    keys: ['全双工', '101 协议升级', 'wss', '心跳保活'],
    src: '计算机网络面试题.md / 农担重点逻辑.md',
  },
  {
    id: 'net-015',
    type: 'essay',
    diff: 'medium',
    sub: 'WebSocket',
    q: '即时通讯的实现方式：短轮询、长轮询、SSE 和 WebSocket 有什么区别？',
    ans: `**面试回答：**

1. **短轮询**：客户端定时发 HTTP 请求问“有没有新消息”。实现简单，但大量无效请求、实时性差、服务端压力大；
2. **长轮询**：请求发出后**服务端挂起**，有数据或超时才响应，客户端收到后立即再发起。比短轮询省请求，但每次仍要重新建连，服务端要维护挂起连接；
3. **SSE**（Server-Sent Events）：基于 HTTP 的**服务端单向推送**，本质是一个 \`Content-Type: text/event-stream\` 的长期 HTTP 响应，消息用 event/data 字段组织，浏览器 EventSource 原生支持、**自带断线重连**；缺点是**单向**（客户端只能靠额外请求发数据）、EventSource 只支持 GET；
4. **WebSocket**：独立协议，握手升级后**全双工**，双方都能主动发消息，开销最小、能力最强，但需要服务端单独实现协议。

**选型**：
- 只需要服务端→客户端的文本流（AI 流式输出、通知、日志）：**SSE 最轻**；
- 需要双向高频交互（聊天、协同编辑、节点状态）：**WebSocket**。

补充：AI 对话里常用 **fetch + ReadableStream 模拟 SSE 读取**，优点是能用 POST、自定义请求头、配合 AbortController 中断。`,
    ana: '四方案按“实时性/方向/开销”三维对比。SSE 优点：轻量、走 HTTP 基础设施、自动重连。',
    keys: ['短轮询', '长轮询', 'text/event-stream', '全双工'],
    src: '计算机网络面试题.md / 收集的面试知识点.md / 知识点快速复习指南.md',
  },
  {
    id: 'net-016',
    type: 'judge',
    diff: 'medium',
    sub: 'WebSocket',
    q: 'SSE 基于 HTTP 协议实现，浏览器原生 EventSource 会在连接断开后自动重连，但原生 EventSource 只能发起 GET 请求。',
    ans: true,
    ana: 'SSE 的响应 Content-Type 为 text/event-stream，EventSource 自带 Last-Event-ID 重连机制。限制：只支持 GET、无法自定义 header（如带 token 常改用 query 或 cookie）。需要 POST/复杂请求时，用 fetch + ReadableStream 手动读取流，配合 AbortController 实现停止生成。',
    keys: ['text/event-stream', '自动重连', 'fetch + ReadableStream'],
    src: '收集的面试知识点.md / 知识点快速复习指南.md',
  },
  {
    id: 'net-017',
    type: 'essay',
    diff: 'medium',
    sub: 'HTTP 协议',
    q: 'HTTP 请求报文和响应报文的结构是什么样的？与缓存相关的请求头有哪些？',
    ans: `**面试回答：**

**请求报文**：
\`\`\`
请求行：方法 URL 版本       →  GET /api/users HTTP/1.1
请求头：Host、User-Agent、Content-Type、Authorization、Cookie...
空行
请求体：POST/PUT 携带的数据
\`\`\`

**响应报文**：
\`\`\`
状态行：版本 状态码 短语    →  HTTP/1.1 200 OK
响应头：Content-Type、Cache-Control、ETag、Set-Cookie...
空行
响应体：HTML/JSON/图片等
\`\`\`

**缓存相关请求头**：
- **强缓存验证**：Cache-Control（max-age/no-cache/no-store）、Expires、Pragma（旧）；
- **协商缓存验证**：If-None-Match（配合响应头 ETag）、If-Modified-Since（配合 Last-Modified）；
- 其他：If-Match、If-Range（断点续传）。

注意：If-None-Match/If-Modified-Since 是**请求头**，ETag/Last-Modified 是**响应头**，配对使用。`,
    ana: 'no-cache 是“可以缓存但必须协商验证”，no-store 是“完全不缓存”，两者区别易考。',
    keys: ['请求行/状态行', 'If-None-Match', 'Cache-Control'],
    src: '计算机网络面试题.md',
  },
  {
    id: 'net-018',
    type: 'single',
    diff: 'easy',
    sub: 'HTTP 协议',
    q: 'OPTIONS 请求方法的主要使用场景是什么？',
    opts: [
      '下载文件',
      'CORS 跨域预检请求，询问服务器允许的方法和请求头',
      '删除资源',
      '长轮询等待消息',
    ],
    ans: 'B',
    ana: 'OPTIONS 用于“询问服务器支持哪些方法/请求头”，最常见于 **CORS 预检**：复杂请求（自定义头、application/json、非简单方法）发出前，浏览器先发 OPTIONS 预检，服务端返回 Access-Control-Allow-Methods/Headers/Origin，通过后才发正式请求。预检结果可用 Access-Control-Max-Age 缓存减少 OPTIONS 次数。',
    keys: ['预检请求', 'Access-Control-*', 'Max-Age 缓存'],
    src: '计算机网络面试题.md',
  },
  {
    id: 'net-019',
    type: 'essay',
    diff: 'easy',
    sub: '网络模型',
    q: 'OSI 七层模型和 TCP/IP 五层协议分别是什么？从输入 URL 发请求涉及哪些层？',
    ans: `**面试回答：**

**OSI 七层**（理论模型，自上而下）：
应用层 → 表示层 → 会话层 → 传输层 → 网络层 → 数据链路层 → 物理层

**TCP/IP 五层**（实用模型）：
1. **应用层**：HTTP、HTTPS、DNS、FTP——为应用提供服务；
2. **传输层**：TCP、UDP——端到端可靠/高效传输；
3. **网络层**：IP、ICMP——路由寻址，把数据包送到目标主机；
4. **数据链路层**：以太网、ARP——相邻节点传输帧；
5. **物理层**：比特流传输。

**一次 HTTP 请求的封装过程**：应用层生成 HTTP 报文 → 传输层加 TCP 头（端口）→ 网络层加 IP 头（地址）→ 链路层加以太网帧头（MAC）→ 物理层转比特流发送；接收方逐层解封装。ARP 协议用于把 IP 解析为 MAC 地址。

面试讲清楚“七层是理论参考、五层是实际落地”即可。`,
    ana: '顺带记：交换机工作在链路层、路由器工作在网络层。',
    keys: ['七层 vs 五层', '封装解封装', 'ARP'],
    src: '计算机网络面试题.md',
  },

  {
    id: 'net-020',
    type: 'multiple',
    diff: 'medium',
    sub: 'HTTP 状态码',
    q: '下列哪些状态码表示**重定向**（3xx）？（多选）',
    opts: ['301', '304', '307', '502'],
    ans: ['A', 'C'],
    ana: '3xx 是重定向：301 永久重定向、302 临时重定向、303 必须改用 GET、307 临时重定向且不允许改变请求方法、308 永久且不改方法。**304 Not Modified 属于缓存协商成功**（不算重定向，浏览器继续用本地缓存）；502 是网关错误（5xx 服务端错误）。401 vs 403 也常一起考：未认证 vs 已认证但无权限。',
    keys: ['3xx 重定向', '304 协商缓存', '307 不改方法'],
    src: '计算机网络面试题.md / 一些高频率考点.md',
  },
  {
    id: 'net-021',
    type: 'multiple',
    diff: 'easy',
    sub: 'TCP 与 UDP',
    q: '关于 TCP 和 UDP 的使用场景，下列说法正确的有哪些？（多选）',
    opts: [
      '网页 HTTP/HTTPS、文件传输适合 TCP（要求可靠）',
      '视频通话、直播、DNS 查询适合 UDP（要求实时）',
      'UDP 保证数据不丢失且有序到达',
      'QUIC（HTTP/3）基于 UDP 实现可靠传输',
    ],
    ans: ['A', 'B', 'D'],
    ana: 'TCP 面向连接、可靠、字节流，适合准确性优先的场景；UDP 无连接、头部仅 8 字节、效率高，适合实时性优先、可容忍少量丢包的场景。UDP **不保证**可靠和有序（这正是选项 C 错误的原因）。QUIC/HTTP3 在 UDP 之上自行实现了可靠传输、流级别独立重传和连接迁移。',
    keys: ['TCP 可靠优先', 'UDP 实时优先', 'QUIC'],
    src: '计算机网络面试题.md',
  },
  {
    id: 'net-022',
    type: 'essay',
    diff: 'medium',
    sub: 'HTTP 缓存',
    q: 'Cache-Control 中 no-cache、no-store、max-age 和 immutable 分别是什么意思？',
    ans: `**面试回答：**

- **max-age=秒数**：强缓存有效期，未过期时浏览器直接使用本地缓存，不发请求；
- **no-cache**：可以缓存，但每次使用前必须向服务器发起协商验证，确认资源没变才能用；它不是“不缓存”；
- **no-store**：完全不缓存，请求和响应都不应被存储，适合敏感数据；
- **immutable**：资源在有效期内不会变化，浏览器刷新时也可以放心使用缓存，适合带 hash 的静态资源。

**项目策略**：index.html 通常 no-cache 或短缓存，保证能及时拿到最新资源入口；带内容 hash 的 JS/CSS/图片设置较长 max-age + immutable。`,
    ana: 'no-cache 和 no-store 是高频混淆点：前者要协商，后者完全不存。',
    keys: ['max-age', 'no-cache 协商', 'no-store 不存储', 'immutable'],
    src: 'HTTP 缓存高频题',
  },
  {
    id: 'net-023',
    type: 'single',
    diff: 'medium',
    sub: 'HTTPS',
    q: 'HTTPS 中数字证书最核心的作用是什么？',
    opts: [
      '让 HTTP 请求体自动变小',
      '证明服务器身份，并防止中间人替换服务器公钥',
      '替代 TCP 三次握手',
      '让浏览器不再需要 DNS 解析',
    ],
    ans: 'B',
    ana: 'HTTPS 握手中服务端会返回证书，证书由受信任 CA 签发，包含域名、服务器公钥、有效期等信息。浏览器验证证书链、域名、有效期和吊销状态，确认公钥确实属于目标服务器，防止中间人把自己的公钥冒充成服务器公钥。',
    keys: ['CA', '证书链', '服务器身份', '防中间人'],
    src: 'HTTPS 高频题',
  },
  {
    id: 'net-024',
    type: 'essay',
    diff: 'medium',
    sub: 'WebSocket',
    q: 'WebSocket 为什么需要心跳机制？断线重连一般怎么设计？',
    ans: `**面试回答：**WebSocket 是长连接，但网络切换、代理超时、服务端重启、移动端后台等都可能导致连接半开：表面上连接还在，实际已经无法收发消息。所以需要心跳检测连接是否可用。

**心跳机制**：客户端定时发送 ping 或业务心跳包，服务端回复 pong；如果连续 N 次超时未收到响应，就认为连接不可用，主动关闭并重连。服务端也可以定期 ping 客户端清理失效连接。

**断线重连设计**：
1. 避免立即无限重连，使用指数退避或固定间隔 + 最大次数；
2. 区分主动关闭和异常断开，用户主动退出不应重连；
3. 重连后带上会话 ID、最后消息 ID 或游标，补拉断线期间缺失消息；
4. 发送中的消息要有本地队列和幂等 ID，避免重连后重复提交；
5. 页面卸载时清理定时器和连接。`,
    ana: '心跳解决半开连接检测，重连要处理退避、主动关闭、消息补偿和幂等。',
    keys: ['心跳 ping/pong', '半开连接', '指数退避', '消息补偿'],
    src: 'WebSocket 稳定性高频题',
  },
]
